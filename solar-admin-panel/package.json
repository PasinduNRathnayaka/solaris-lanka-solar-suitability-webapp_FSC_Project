{
  "name": "solar-admin-panel",
  "version": "1.0.0",
  "description": "Solar Energy Admin Panel - MERN Stack",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server --prefix backend\" \"npm run dev --prefix frontend\"",
    "server": "npm run dev --prefix backend",
    "client": "npm run dev --prefix frontend",
    "install-deps": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix backend"
  },
  "keywords": ["solar", "energy", "admin", "mern", "mongodb", "react", "node"],
  "author": "Your Name",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
